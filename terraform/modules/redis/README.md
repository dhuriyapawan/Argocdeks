# Redis Terraform Module

This module sets up:
- AWS ElastiCache Redis replication group
- Custom ElastiCache subnet group mapping to database subnets
- Transit and at-rest encryption using KMS keys
- Redis Client Authentication (Auth Token) enabled
