apiVersion: v1
kind: Service
metadata:
  name: ride-service
  namespace: rideshare
  labels:
    app: ride-service
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: ride-service
  type: ClusterIP
