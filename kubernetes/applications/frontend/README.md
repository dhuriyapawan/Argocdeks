# Frontend Application Manifests

In production, the frontend microservice is deployed via its Helm chart (`helm/frontend/`) managed by ArgoCD.

Refer to the Helm chart for deployments, configurations, and service setup.
